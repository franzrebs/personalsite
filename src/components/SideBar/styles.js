import { css } from '@emotion/core';

const root = css`
  width: 210px;
  padding: 56px 40px 0;
`;

const siteName = css`
  font-weight: 600;
  font-size: 1.2em;
`;

export default { root, siteName };
