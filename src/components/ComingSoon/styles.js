import { css } from '@emotion/core';

const root = css`
  display: flex;
  align-items: center;
  justify-content: center;
  height: calc(100vh - 112px);
`;

const text = css`
  color: hsla(0, 0%, 0%, 0.5);
`;

export default {
  root,
  text,
};
